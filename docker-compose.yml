services:
  api:
    build: .
    ports:
      - "8000:8000"
    command: uvicorn backend.app.main:app --host 0.0.0.0 --port 8000
  dashboard:
    build: .
    ports:
      - "8501:8501"
    command: streamlit run frontend/dashboard/app.py --server.address 0.0.0.0
    depends_on:
      - api
